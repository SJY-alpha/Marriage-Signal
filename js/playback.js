import { state } from './app.js';
import { DOM_ELEMENTS } from './config.js';
import { showToast } from './ui.js';

export function stopSinglePlayback() {
    // ... 원본 stopSinglePlayback 함수 내용
}

export async function playSingleDialogue(dialogue, controlsElement) {
    // ... 원본 playSingleDialogue 함수 내용
}

export function playTimeline() {
    // ... 원본 playTimeline 함수 내용
}

export function pauseTimeline() {
    // ... 원본 pauseTimeline 함수 내용
}

export function stopTimeline() {
    // ... 원본 stopTimeline 함수 내용
}

function scheduleAudio() {
    // ... 원본 scheduleAudio 함수 내용
}

function updatePlaybackFrame() {
    // ... 원본 updatePlaybackFrame 함수 내용
}
